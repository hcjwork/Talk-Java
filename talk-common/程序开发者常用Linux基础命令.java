一、文件和目录操作
1、`ls`命令
列出当前目录的所有文件（linux中目录也是文件）。`ll`命令也有类似功能。
默认只展示文件名称，展示细节时文件大小默认以字节为单位。
常用场景：查看文件的读写权限、查看文件的创建时间、查看是否包含指定文件
完整命令：ls [OPTION]... [FILE]...
OPTION：所支持的选项，具体可通过`ls --help`查看。

重点示例1：ls -l
说明：列出当前目录下的所有文件。-l表示列出细节信息。
输出示例：
total 16501
drwxr-xr-x  6 root  root       4096 May 20 16:25 apache-maven-3.6.3
-rw-r--r--  1 root  root      12405 May 21 12:03 application.properties

重点示例2：ls -lht
说明：列出当前目录下的所有文件，文件的大小以人类可读的单位展示，并按照时间倒序排列。
输出示例：
total 17k
-rw-r--r--  1 root  root   13K May 21 12:03 application.properties
drwxr-xr-x  6 root  root  4.0K May 20 16:25 apache-maven-3.6.3

2、`cd`命令
切换到指定目录。使用非常频繁的一种命令，简单易用。
完整命令：cd [-L|[-P [-e]] [-@]] [dir]

3、`pwd`命令
显示当前目录的全路径。也比较常用，比如想要拷贝关键文件所处全路径时，用pwd查看下复制出来。
完整命令：pwd [-LP]

4、`mkdir`命令
创建目录。简单命令，很常用，支持递归创建目录，即可以一次创建多层目录，多层目录用/分隔。
完整命令：mkdir [OPTION]... DIRECTORY...
OPTION：支持的动作选项，可通过`mkdir --help`查看详情。

重点示例1：mkdir test
说明：在当前目录下创建单层目录test。

重点示例2：mkdir -p test2/test001
说明：在当前目录下创建多层目录。

5、`rm`命令
删除文件或目录。支持递归删除和强制删除，注意强制删除要慎用。
完整命令：rm [OPTION]... [FILE]...
OPTION：支持的动作选项，可通过`rm --help`查看详情。

重点示例1：rm t1.txt
说明：删除t1.txt文件，非强制删除，删除时需要输入yes/y确认。文件默认在当前目录寻找，可以指定为全路径。

重点示例2：rm -r test2/test001
说明：递归删除目录和目录中的所有文件，包括子目录。-r只能用于目录删除，不能用于文件删除。

重点示例3：rm -rf test
说明：强制递归删除指定目录。-f是-force，强制执行。

6、`cp`命令
复制文件或目录。支持递归复制。很常用，尤其是做数据备份时，比如服务部署、服务配置调整等。
完整命令： 
cp [OPTION]... [-T] SOURCE DEST
或 cp [OPTION]... SOURCE... DIRECTORY
或 cp [OPTION]... -t DIRECTORY SOURCE...
OPTION：支持的动作选项，可通过`cp --help`查看详情。

重点示例1：cp -a test test2
说明：递归复制目录。

重点示例2：cp -r test test3
说明：递归复制目录。

重点示例3：cp t.txt ../test4/t2.txt
说明：将当前目录的t.txt文件复制到上一层级下的test4目录下，并重命名文件为t2.txt。

7、`mv`
移动或重命名文件。

8、`find`
搜索文件。

9、`grep`
文本搜索。

二、文件查看与编辑
1、`cat`
查看文件内容。一次性全部展示。

2、`more`
分页查看文件。

3、`less`
分页查看文件。

4、`tail`
查看文件尾部。

5、`head`
查看文件头部。

6、`vim`
编辑文件。

7、`diff`
比较文件差异。

三、用户和权限
1、`chmod`
修改文件权限。

2、`chown`
修改文件的所属用户。

3、`sudo`
以超级用户权限执行命令。

4、`passwd`
修改密码。

5、`id`
查看用户信息。

四、进程与系统监控
1、`ps`
查看进程。

2、`top`
实时监控系统资源。

3、`kill`
终止进程。

4、`netstat` / `ss`
查看网络连接。

5、`df`
查看磁盘空间。

6、`du`
查看目录大小。

7、`free`
查看内存使用情况。

五、网络操作相关命令
1、`ping`
测试网络连通性。

2、`curl` 
发起API请求。

3、`wget`
下载文件。

4、`ssh`
远程登录服务器。

5、`scp`
安全拷贝文件。

6、`ifconfig`
查看网络接口信息。

7、`ip`
查看ip相关信息。

六、系统信息查看
1、`uname`
查看系统信息。

2、`date`
显示或设置时间。

3、`history`
查看命令历史。

4、`alias`
设置命令别名。

七、开发调试常用命令
1、`tar`
压缩或解压文件。

2、`awk`
文本处理（列提取/统计）。

3、`wc`
统计各项数据。例如统计文件行数、统计文件大小、统计文件字符数。
